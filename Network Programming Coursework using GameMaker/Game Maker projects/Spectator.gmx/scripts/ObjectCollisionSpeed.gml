/*currentSpd = argument[0];
impactAngle = argument[1];

e = 0.1; //coefficient of restitution on all objects is constant

postImpactSpd = sqrt(power(currentSpd, 2)*(power(cos(impactAngle), 2) + power(e, 2)*power(sin(impactAngle), 2)));

return postImpactSpd;

impactAngle = argument[0];

e = 0.1;

reflectionAngle = arctan(e*tan(impactAngle));

return reflectionAngle;*/
